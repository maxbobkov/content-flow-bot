version: '3.8'
services:
  bot:
    build: .
    volumes:
      - ./data:/app/data
    env_file:
      - .env 
    restart: always
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - CHANNEL_ID=${CHANNEL_ID}